import React, { Component } from 'react';
import SideBar from './SideBar';

class MyPlaces extends Component {

    render() {
        return(
            <div>
                
            </div>       
        )
    }

    /*
    render() {
        return (
            <li role="button" className="box" tabIndex="0" 
            onKeyPress={this.props.openInfoWindow.bind(this, this.props.data.marker)} 
            onClick={this.props.openInfoWindow.bind(this, this.props.data.marker)}></li>
        );
        
    }
    */

}

export default MyPlaces;